def hamming_weight(x):
    x = bin(x) [2:]
    x = list(x)
    count = 0
    for i in x:
        if i == "1":
            count += 1
        else:
            count += 0
    return count



#Best practice:
def hamming_weight(x): return bin(x).count("1")
